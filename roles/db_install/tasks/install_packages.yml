---
- name: Install database Packages(Here we use Mariadb as the database service)
  ansible.builtin.dnf:
    name: "{{ db_install_packages }}"
    state: present
  when: ansible_facts['os_family'] == "RedHat"
